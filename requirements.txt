# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# --- Config & validation ---
pydantic>=2.8,<3.0
pydantic-settings>=2.4
email-validator>=2.0

# --- Database ---
sqlalchemy[asyncio]>=2.0.30
aiosqlite>=0.20

# --- Auth ---
pyjwt>=2.9

# --- LangChain / LangGraph (verified against core 1.5.x / langgraph 1.2.x) ---
langchain-core>=1.0
langgraph>=1.0
langchain-text-splitters>=1.0

# --- Vector store (Chroma persistent; numpy powers the in-memory fallback) ---
numpy>=1.26
langchain-chroma>=1.0
chromadb>=1.0

# --- LLM providers (install the one(s) you use) ---
langchain-openai>=1.0
# langchain-google-genai>=3.0
# langchain-ollama>=1.0

# --- HTTP client (Slack / Twilio integrations) ---
httpx>=0.27

# --- Testing ---
pytest>=8.2
pytest-asyncio>=1.0
